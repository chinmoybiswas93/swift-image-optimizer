{
	"name": "swift-image-optimizer",
	"version": "1.1.0",
	"description": "Admin interface for the Swift Image Optimizer WordPress plugin.",
	"license": "GPL-2.0-or-later",
	"private": true,
	"scripts": {
		"build": "wp-scripts build",
		"start": "wp-scripts start",
		"lint:js": "wp-scripts lint-js",
		"lint:php": "command -v phpcs >/dev/null || { echo 'phpcs not found on PATH. WPCS is installed outside this plugin so the committed vendor/ stays autoloader-only - see context/specs/done/09-phpcs-compliance.md.'; exit 1; }; phpcs --standard=phpcs.xml.dist -s",
		"test:e2e": "playwright test",
		"test:php": "tests/php/run.sh"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@wordpress/scripts": "^30.0.0"
	}
}
