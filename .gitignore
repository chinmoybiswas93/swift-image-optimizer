# Dependencies
node_modules/
vendor/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build/ is committed deliberately: WordPress.org distributes the plugin as-is,
# so the compiled admin bundle has to ship with it.

# Zip artifacts from bin/build-dist.sh
dist/

# Playwright / test output
test-results/
playwright-report/
blob-report/
playwright/.cache/
coverage/

# PHPCS cache
.phpcs.cache
.phpunit.result.cache

# Env
.env
.env.local
